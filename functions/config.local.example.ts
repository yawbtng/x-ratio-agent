// Copy this to config.local.ts and fill in your values (or run `pnpm setup`, which writes it).
// config.local.ts is gitignored so your personal Context/handle never land in git.
//
//   CONTEXT_ID — a Browserbase Context holding your logged-in X session. Create one with
//                `pnpm auth` (opens a Live View, you log into X by hand, the id is printed).
//   X_HANDLE   — your X username without the @.
export const CONTEXT_ID = "REPLACE_WITH_YOUR_BROWSERBASE_CONTEXT_ID";
export const X_HANDLE = "REPLACE_WITH_YOUR_X_HANDLE";
