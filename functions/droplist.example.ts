// EXAMPLE ONLY — shows the shape. The real functions/droplist.ts is generated from YOUR
// data/scored.json by `node scripts/gen-droplist.mjs` (run for you by `pnpm setup`) and is
// gitignored. The Function unfollows only handles in DROP that you STILL follow at run time,
// so it is naturally idempotent/resumable.
export const DROP: string[] = ["example_handle_you_no_longer_want_to_follow"];
