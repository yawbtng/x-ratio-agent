node_modules/
dist/
.env
# NOTE: data/ IS committed (private repo only) so the GitHub Actions cron can resume
# grind state between runs. It holds your follow graph + the LLM keep/drop judgments +
# the Browserbase contextId (not credentials — the X login lives server-side in Browserbase).

# status notifier runtime artifacts (local only)
data/.status-cache.json
data/status-log.txt
data/status-cron.out
data/status-cron.err
